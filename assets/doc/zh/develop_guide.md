# 开发指南

## 配置环境

```bash
#(推荐)
conda create -n aalc
conda activate aalc
conda install python=3.12
# 安装依赖
python -m pip install --upgrade pip
pip install -r requirements.txt
# 编译开发版本
python main_dev.py
```